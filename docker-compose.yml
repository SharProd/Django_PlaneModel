version: "3"
services:
  db:
    image: postgres
    container_name: PlaneModel_db
    volumes:
      - ./data/db:/var/lib/postgresql/data
#    environment:
#      POSTGRES_PASSWORD: postgre
    env_file:
      - .env.dev
    ports:
      - '5432:5432'
    expose:
      - '5432'
  web:
    build: .
    container_name: PlaneModel_web
    command: >
      bash -c "python PlaneModel/manage.py makemigrations
      && python PlaneModel/manage.py migrate
      && python PlaneModel/manage.py runserver 0.0.0.0:8080"
    volumes:
      - .:/code
    env_file:
      - .env.dev
    ports:
      - "8080:8080"
    depends_on:
      - db